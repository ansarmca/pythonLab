# Input: List of integers from the user
numbers = list(map(int, input("Enter integers separated by spaces: ").split()))

# a. Filter even numbers using list comprehension
even_numbers = [num for num in numbers if num % 2 == 0]

# b. Create a new list with cubes of odd numbers
cubes_of_odd_numbers = [num**3 for num in numbers if num % 2 != 0]

# Display the results
print("\nEven numbers:", even_numbers)
print("Cubes of odd numbers:", cubes_of_odd_numbers)

def get_positive_numbers(numbers):
  positive_numbers = []
  for number in numbers:
    if number > 0:
      positive_numbers.append(number)
  return positive_numbers

numbers = [1, -2, 3, -4, 5, 0]
positive_list = get_positive_numbers(numbers)
print(positive_list)

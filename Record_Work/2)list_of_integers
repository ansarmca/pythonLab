numbers = input("Enter a list of integers separated by spaces: ").split()
new_list = []
for num in numbers:
  if int(num) > 100:
    new_list.append('over')
  else:
    new_list.append(num)

print(new_list)
